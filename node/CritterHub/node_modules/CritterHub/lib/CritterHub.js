'use strict';

var CritterHub = function() {
	console.log('CritterHub!');
	
	var _socket;
	
	var serial = require('./connectors/SerialConnector.js')(this);
	
	this.setSocket = function(socket) {
		_socket = socket;
		_socket.emit('data', 'foo');
	};
	
	this.data = function(json) {
		//console.log('-->json', json);
		if (_socket !== undefined) {
			_socket.emit('data', json);
		}
	};
};

module.exports = new CritterHub();